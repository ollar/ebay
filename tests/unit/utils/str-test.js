import str from 'ebay/utils/str';
import { module, test } from 'qunit';

module('Unit | Utility | str', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = str();
    assert.ok(result);
  });
});
