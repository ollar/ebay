import trace from 'ebay/utils/trace';
import { module, test } from 'qunit';

module('Unit | Utility | trace', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = trace();
    assert.ok(result);
  });
});
