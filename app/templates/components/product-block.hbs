<div class="section">
    <h3 class="title">
        {{#if nolink}}
            {{product.title}}
        {{else}}
            {{link-to product.title 'index.product-details' product}}
        {{/if}}

        <div class="bids" title={{t 'index.product-details.bids'}}>
            ({{bidsNumber}})
        </div>
    </h3>

    {{#swiper-container}}
        {{#each product.images as |image|}}
            <div class="swiper-slide">
                <img src={{image.base64}} width={{image.width}} height={{image.height}}>
            </div>
        {{/each}}
    {{/swiper-container}}

    <div class="description">
        {{product.description}}
    </div>

    <div class="price">
        {{price product.price}}
    </div>

    {{yield}}
</div>